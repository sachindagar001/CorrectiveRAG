# === CRAG Agent — Dependencies ===
# Tested with Python 3.10+ (built on 3.12)

# Core LLM orchestration
langgraph>=1.0.0
langchain-core>=1.0.0
langchain-community>=0.3.0
langchain-text-splitters>=1.0.0

# LLM API (OpenRouter is OpenAI-compatible — we use raw `requests`)
requests>=2.31.0

# Embeddings & vector store
sentence-transformers>=3.0.0
faiss-cpu>=1.7.4

# Web search (ddgs is the new package name; duckduckgo-search is legacy fallback)
ddgs>=9.0.0
duckduckgo-search>=6.0.0

# ML / DL
torch>=2.0.0
transformers>=4.40.0

# UI
streamlit>=1.30.0
streamlit-mermaid>=0.3.0

# Utilities
pydantic>=2.0.0
python-dotenv>=1.0.0
pandas>=2.0.0
plotly>=5.18.0
graphviz>=0.20.0
arxiv>=2.1.0
numpy>=1.24.0
tqdm>=4.66.0

# Testing
pytest>=8.0.0
